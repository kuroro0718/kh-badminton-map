<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Kaohsiung Badminton Map</a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to "新增球聚", new_meetup_path %></li>
        
        <form class="navbar-form navbar-left" role="search">
          <%= form_tag(meetups_path, :method => "get", id: "search-form") do %>
            <div class="form-group">
              <%= text_field_tag :search, params[:search], placeholder: "依球聚地點尋找", class: 'form-control' %>               
            </div>
            <%= submit_tag "Search", class: "btn btn-default" %> 
          <% end %>
        </form>

        <% if !current_user %>
          <li><%= link_to "登入", new_user_session_path %></li>  
          <li><%= link_to "Facebook登入", user_omniauth_authorize_path(:facebook) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hi <%= current_user.name %> <span class="caret"></span></a>    
            <ul class="dropdown-menu">
              <li><%= link_to "登出", destroy_user_session_path, method: :delete %></li>
              <li><%= link_to "帳戶設定", edit_user_registration_path %></li>
            </ul>
          </li>           
        <% end %>      
      </ul>      
    </div>  
  </div>
</nav>